#include <stdio.h>

int main(void)
	/* The function used here prints the string without having to use a newline character */
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
