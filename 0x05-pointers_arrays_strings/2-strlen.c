#include "main.h"
#include <string.h>
/**
 * _strlen - calculates string length
 * @s: the string
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
