#include <stdio.h>
/**
 * main - Prints the upper and lower
 * case alphabets
 * Return: 0
 *
 */
int main(void)
       	<= 'z'; a++);

	for (A = 'A'; A <= 'Z'; A++);
	putchar(a);
	putchar(A);
	return (0);
}
