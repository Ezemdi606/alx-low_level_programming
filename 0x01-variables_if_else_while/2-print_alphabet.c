#include <stdio.h>

/**
 * main - Prints alphabets in lowercase
 *
 * Return: 0
 *
 */
int main(void)
{
	char letter;
	int i;
	
for (letter = 'a'; letter <= 'z'; letter++);

	{
		putchar(letter);
		putchar('\n');
	}

	return (0);
}
